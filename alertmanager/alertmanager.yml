global:
  resolve_timeout: 1m

route:
  receiver: 'decision-engine-webhook'
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: 'decision-engine-webhook'
    webhook_configs:
      - url: 'http://localhost:6000/alert'
        send_resolved: true